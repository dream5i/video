[project]
name = "new-project-api"
version = "0.1.0"
description = "FastAPI orchestration layer for 全新项目"
requires-python = ">=3.11"
dependencies = [
  "alembic>=1.16.0",
  "fastapi>=0.116.0",
  "pydantic>=2.11.0",
  "sqlalchemy>=2.0.40",
  "uvicorn>=0.35.0",
]

[tool.uv]
package = false
